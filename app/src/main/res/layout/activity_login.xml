<?xml version="1.0" encoding="utf-8"?>
<!-- Prijava — Sunny pop onboarding. -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient"
    android:fillViewport="true"
    tools:context=".ui.LoginActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="32dp"
        android:paddingBottom="24dp">

        <include layout="@layout/view_brand" />

        <TextView
            android:id="@+id/lgLoginTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="22dp"
            android:fontFamily="@font/baloo2"
            android:text="Dobrodošli nazad"
            android:textColor="@color/text"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="26dp"
            android:fontFamily="@font/nunito"
            android:text="Prijavi se i nastavi takmičenje"
            android:textColor="@color/muted"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView style="@style/FieldLabel" android:text="EMAIL ILI KORISNIČKO IME" />

        <EditText
            android:id="@+id/lgInsertCredentials"
            style="@style/FieldInput"
            android:layout_marginBottom="16dp"
            android:hint="npr. milos@primer.rs"
            android:inputType="textEmailAddress" />

        <TextView style="@style/FieldLabel" android:text="LOZINKA" />

        <EditText
            android:id="@+id/lgInsertPassword"
            style="@style/FieldInput"
            android:hint="••••••••"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/lgLoginButton"
            style="@style/Widget.Slagalica.Button"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:layout_marginTop="28dp"
            android:text="Prijavi se"
            android:textSize="16sp"
            app:cornerRadius="@dimen/radius"
            xmlns:app="http://schemas.android.com/apk/res-auto" />

        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:minHeight="24dp" />

        <TextView
            android:id="@+id/rgLoginLink"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="24dp"
            android:fontFamily="@font/nunito"
            android:text="Nemaš nalog?  Registruj se"
            android:textColor="@color/accent"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>
</ScrollView>
